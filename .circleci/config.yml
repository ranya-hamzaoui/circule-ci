version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:20.9.0

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm install

      - run:
          name: Run tests
          command: npm run test -- --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox

workflows:
  version: 2
  build:
    jobs:
      - build